// Inner content padding only — side gutters live on .app-content
export const PAD = "clamp(18px, 3.2vw, 36px)"

// Consistent gap between homepage sections (label top spacing)
export const SECTION_GAP = 40
